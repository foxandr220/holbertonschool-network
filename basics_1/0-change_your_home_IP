#!/usr/bin/env bash
# Change DNS resolutions

# Update hosts file - replace 127.0.0.1 with 127.0.0.2 for localhost
sed -i 's/^127\.0\.0\.1\s*localhost/127.0.0.2\tlocalhost/' /etc/hosts

# Add facebook.com resolution
echo "8.8.8.8    facebook.com" >> /etc/hosts
